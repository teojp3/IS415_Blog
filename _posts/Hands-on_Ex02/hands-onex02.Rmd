---
title: "Hands-on_Ex02"
description: |
  A short description of the post.
author:
  - name: Teo Jun Peng
    url: https://teojp3-is415.netlify.app/
date: 08-27-2021
output:
  distill::distill_article:
    self_contained: false
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r}
packages = c('sf', 'tidyverse')
for (p in packages){
  if(!require(p, character.only = T)){
    install.packages(p)
  }
  library(p,character.only = T)
}
```
```{r}
mpsz = st_read(dsn = "data/handson2data/geospatial", 
                  layer = "MP14_SUBZONE_WEB_PL")
```
```{r}
cyclingpath = st_read(dsn = "data/handson2data/geospatial", 
                         layer = "CyclingPath")
```

```{r}
preschool = st_read("data/handson2data/geospatial/pre-schools-location-kml.kml")
```
```{r}
st_geometry(mpsz)
glimpse(mpsz)
```
```{r}
head(mpsz, n=5)
```
```{r}
plot(mpsz)
```
```{r}
plot(st_geometry(mpsz))
```
```{r}
plot(mpsz["PLN_AREA_N"])
```
```{r}
st_crs(mpsz)
```
```{r}
mpsz3414 <- st_set_crs(mpsz, 3414)
st_crs(mpsz3414)
```
```{r}
preschool3414 <- st_transform(preschool, 
                              crs = 3414)
```

```{r}
listings <- read_csv("data/aspatial/listings.csv")
glimpse(listings) 
```

```{r}
listings_sf <- st_as_sf(listings, 
                       coords = c("longitude", "latitude"),
                       crs=4326) %>%
  st_transform(crs = 3414)
```

```{r}
glimpse(listings_sf)
```

```{r}
buffer_cycling <- st_buffer(cyclingpath, 
                               dist=5, nQuadSegs = 30)
buffer_cycling$AREA <- st_area(buffer_cycling)
sum(buffer_cycling$AREA)
```

```{r}
mpsz3414$`PreSch Count`<- lengths(st_intersects(mpsz3414, preschool3414))
```

```{r}
summary(mpsz3414$`PreSch Count`)
```
```{r}
top_n(mpsz3414, 1, `PreSch Count`)
```
```{r}
mpsz3414$Area <- mpsz3414 %>%
  st_area()
```

```{r}
mpsz3414 <- mpsz3414 %>%
  mutate(`PreSch Density` = `PreSch Count`/Area * 1000000)
```

```{r}
hist(mpsz3414$`PreSch Density`)
```
```{r}
ggplot(data=mpsz3414, 
       aes(x= as.numeric(`PreSch Density`)))+
  geom_histogram(bins=20, 
                 color="black", 
                 fill="light blue") +
  labs(title = "Are pre-school even distributed in Singapore?",
       subtitle= "There are many planning sub-zones with a single pre-school, on the other hand, \nthere are two planning sub-zones with at least 20 pre-schools",
      x = "Pre-school density (per km sq)",
      y = "Frequency")
```
```{r}
ggplot(data=mpsz3414, 
       aes(y='PreSch Count', 
           x= as.numeric(`PreSch Density`))) +
  geom_point(color="black", 
                 fill="light blue") +
  xlim(0, 60) +
  ylim(0. 60) +
  labs(title = "",
       subtitle= "There are many planning sub-zones with a single pre-school, on the other hand, \nthere are two planning sub-zones with at least 20 pre-schools", x = "Pre-school density (per km sq)" , 
      y = "Pre-school count")
```


